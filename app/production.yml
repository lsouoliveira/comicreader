version: "2.2"
services:
  comic_reader_app:
    build:
      context: .
      dockerfile: Dockerfile.build
    container_name: comic_reader_app
    env_file: .env.production
    ports:
      - "80:80"
      - "8888:8888"
    volumes:
      - ".:/app"
